# The Movie Database (TMDB) API Key (Optional)
# If provided, the system will pull live movie details, categories, and posters.
# If omitted/blank, the system runs in local fallback mode using movies_metadata.csv.
TMDB_API_KEY=

# Base URL of the FastAPI backend service
API_BASE=http://127.0.0.1:8000
